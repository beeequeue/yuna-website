mixin lazyImg(file)
  img.image.lazy(
    src="/img/flow-small/" + file + ".jpg"
    data-src="/img/flow/" + file + ".jpg"
  )

<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    meta(name="theme-color" content="#212d47")

    style.
      body { background: #111; color: #eee; transition: background 1s; }

    link(rel="stylesheet", href="/css/index.css")

    title Yuna - Anime Player and List Manger

  body
    header
      img.logo(src="/img/logo.png")

    .section
      .content Watch anime and update your List from the same place!
      +lazyImg("anime")

    .section
      .content Text goes here 2
      +lazyImg("anime-spoiler")

    .section
      .content Text goes here 3
      +lazyImg("list")

    .below-fold.
      No one can see this :(

    footer=isProd

    script(src="/js/index.js")
